{
    "buildCommand": "cd client && npm install && npm run build",
    "outputDirectory": "client/.next",
    "framework": "nextjs",
    "rewrites": [
        {
            "source": "/socket.io/(.*)",
            "destination": "https://pokemon-battle-platform-server.onrender.com/socket.io/$1"
        }
    ]
}